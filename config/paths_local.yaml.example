# @package _global_
# Local path configuration (gitignored)
# 
# Usage:
# 1. Copy this file to paths_local.yaml: cp paths_local.yaml.example paths_local.yaml
# 2. Update project_root to your actual project path
# 3. Or run: bash setup_paths.sh (it will create this file automatically)
#
# Note: paths_local.yaml is gitignored and will not be committed

# Project root directory - update this to your local path
project_root: /path/to/Oracle-Guided-RL
