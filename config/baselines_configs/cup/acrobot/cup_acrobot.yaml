# @package _global_
defaults:
  - /base_configs/cup_dmc
  - /oracles/dmc/acrobot_swingup_sparse
  - _self_
method_name: "CUP"

seed: 42

env:
  domain_name: "acrobot"
  task_name: "swingup_sparse"

obs_config:
  orientations: [4]
  velocity: [2]

run_name: ${method_name}_${env.domain_name}_${env.task_name}_seed${seed}

training:
  use_wandb: true
  save_video: true


