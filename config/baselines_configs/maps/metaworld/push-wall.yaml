# @package _global_
defaults:
  - /base_configs/maps_metaworld
  - /oracles/metaworld/push-wall
  - _self_
method_name: "MAPS"

seed: 42

env:
  env_name: "push-wall-v3"
  camera_to_render: "corner4"
  seed: ${seed}

run_name: ${method_name}_${env.env_name}_seed${seed}

training:
  n_oracles: 2


